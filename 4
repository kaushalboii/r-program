# Set the seed for reproducibility
set.seed(123)

# Create a vector of 10 random integer values between -50 and 50
random_vector <- sample(-50:50, 10, replace = TRUE)

# Print the random vector
cat("Random vector:", random_vector, "\n")
